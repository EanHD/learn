#!/usr/bin/env python3
"""
LEARN — The all-in-one coding mentor
Unified CLI with Rich output formatting
"""

import sys
from pathlib import Path

# Add CLI directory to path
cli_dir = Path(__file__).parent
sys.path.insert(0, str(cli_dir))


def main():
    """Main entry point - unified CLI"""
    from learn_cli import main as cli_main

    # Learn directory is parent of CLI directory
    cli_main()


if __name__ == "__main__":
    main()
